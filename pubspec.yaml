name: mihr_ui
description: >-
  A complete Flutter UI kit with design tokens, semantic theme extensions,
  automatic light/dark mode, WCAG-compliant palette generation, and
  production-ready components.
version: 0.1.0-dev.1
homepage: https://github.com/mrasityilmaz/mihr_ui
repository: https://github.com/mrasityilmaz/mihr_ui
issue_tracker: https://github.com/mrasityilmaz/mihr_ui/issues

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ">=7.0.0 <11.0.0"


flutter:
  uses-material-design: true
